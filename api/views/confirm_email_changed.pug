extends base

block content
  p.text-lg Hi #{name},
  p.text-lg.font-light.my-6 To complete the process of changing your email address, you must confirm your new address below:
  form(action=`${url}`, method="post" target="_blank")
    input.w-full(value=`${email}` name="email" type="text")
    button.block.btn.btn-link.text-center(type="submit") Confirm email
  .h-1.bg-gray-200.w-full.my-10